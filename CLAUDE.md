# my-blog

마크다운 파일을 읽어서 정적 블로그 웹사이트로 변환하는 프로젝트.

## 목표

- 마크다운으로 글을 쓰면 자동으로 블로그 페이지로 변환됨
- 깔끔하고 읽기 좋은 디자인 (가독성 최우선)
- 다크 모드 지원
- 모바일 반응형
- **프레임워크 없이 순수 HTML/CSS/JavaScript로만 구현** (React, Vue, 정적 사이트 생성기, 번들러 등 사용 안 함)

## 기술 스택 / 제약

- 순수 HTML, CSS, Vanilla JS만 사용. npm 패키지, 빌드 툴체인 도입 금지.
- 마크다운 파싱은 외부 라이브러리 없이 직접 파서를 작성하거나, CDN 없이 동작해야 하는 제약을 고려해 최소한의 자체 구현으로 처리 (필요 시 상의 후 결정).
- 브라우저에서 바로 열어 동작하거나(file://), 간단한 정적 서버로 서빙 가능해야 함.

## 예상 디렉토리 구조

```
my-blog/
  index.html          # 글 목록 페이지
  post.html           # 개별 글 렌더링 페이지 (또는 빌드 시 정적 생성)
  posts/              # 마크다운 원본 (*.md)
  assets/
    css/
      style.css       # 라이트/다크 공통 + 테마 변수
    js/
      markdown.js     # 마크다운 -> HTML 변환 로직
      theme.js        # 다크모드 토글 및 localStorage 저장
      main.js         # 글 목록 로딩, 라우팅 등
```

실제 구조는 구현 진행하면서 확정.

## 디자인 원칙

- 타이포그래피 중심의 깔끔한 레이아웃, 본문 가독성 우선 (적절한 줄간격, 최대 본문 너비 ~65-75ch)
- 다크 모드: `prefers-color-scheme` 기본 감지 + 수동 토글, 선택값은 `localStorage`에 저장
- CSS 변수(`:root`, `[data-theme="dark"]`)로 라이트/다크 색상 관리
- 모바일 퍼스트 반응형 (미디어 쿼리로 데스크톱 확장)
- 불필요한 장식/애니메이션 지양, 콘텐츠가 주인공

## 작업 방식

- 새 기능을 추가할 때마다 실제 브라우저(Browser 도구)로 열어서 라이트/다크 모드, 모바일 뷰 모두 확인 후 완료 보고
- 프레임워크나 빌드 도구를 추가하고 싶어지는 상황이 생기면, 먼저 사용자에게 확인받고 진행 (기본 원칙은 "노 프레임워크")
